package control;

import boardifier.control.ControllerMouse;
import boardifier.model.Model;
import boardifier.view.View;

public class ControllerHoleMouse extends ControllerMouse {
    public ControllerHoleMouse(Model model, View view, KamisadoController kamisadoController) {
        super(model, view, kamisadoController);
    }
}
